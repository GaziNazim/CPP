#include<stdio.h>
#include<math.h>
int  main()
{
    char  eq[3][20];
    int a[3],b[3],c[3],d[3];
    gets(eq1);
    gets(eq2);
    gets(eq3);
    for(int i=0;i<3;i++)
    {
        for(int j=0;eq[i][j]!='\0';j++)
        {
            if(eq[i][j]>=48&&eq[i][j]<=57)

        }
    }

    return 0;
}
